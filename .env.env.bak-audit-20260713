# Development(Local) Env File
# ------------------------------------------------------------------------------

# ! Don't commit this file in real project if contains sensetive information

DEBUG=True
DJANGO_ENVIRONMENT="local"
# SECRET_KEY dihapus dari .env — gunakan env variable atau fallback otomatis Django

# Update this with the actual base URL of your application
BASE_URL = 'http://127.0.0.1:8001'

# ==========================================================================
# Multi-Tenant (Isolated Schema) Ã¢â‚¬â€ PostgreSQL Configuration
# ==========================================================================

DB_ENGINE=postgresql
DB_NAME=sims_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# ==========================================================================
# Internal API Key Ã¢â‚¬â€ Untuk komunikasi dengan CLS (Central License Server)
# ==========================================================================

SAAS_INTERNAL_API_KEY=sk-sims-internal-7f3a9b2e4d1c8f5a6e0b3d9c7a2f4e8d

# ==========================================================================
# Cache Configuration
# ==========================================================================
# Set CACHE_BACKEND=redis untuk aktifkan Redis cache (production)
# Default: locmem (development lokal)
CACHE_BACKEND=locmem
# CACHE_BACKEND=redis
REDIS_URL=redis://127.0.0.1:6379/1
