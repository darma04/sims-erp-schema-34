# =============================================================================
# PRODUCTION ENVIRONMENT EXAMPLE — copy to server as `.env` (never commit real secrets)
# Generated: audit-fix pipeline 2026-07-13
# =============================================================================
# WAJIB SEBELUM GO-LIVE:
# 1. DEBUG=False
# 2. SECRET_KEY unik (generate: python -c "import secrets; print(secrets.token_urlsafe(50))")
# 3. Password DB kuat
# 4. ALLOWED_HOSTS = domain production (koma-separated, tanpa spasi berlebih)
# 5. BASE_URL = https://your-domain
# =============================================================================

DEBUG=False
DJANGO_ENVIRONMENT="production"
BASE_URL=https://your-domain.com
DB_ENGINE=postgresql
DB_NAME=sims_db
DB_USER=app_user
DB_PASSWORD=CHANGE_ME_STRONG_PASSWORD
DB_HOST=127.0.0.1  # or managed DB host
DB_PORT=5432
SAAS_INTERNAL_API_KEY=CHANGE_ME
CACHE_BACKEND=locmem
REDIS_URL=redis://127.0.0.1:6379/0
SECRET_KEY=AXkW9AGj84xSBdzVD3noS1BzDs7ov2-LWTxw2Fw-XSXV4bPR6UiULg  # REPLACE
ALLOWED_HOSTS=your-domain.com

# End of production example
